#include <stdio.h>
#include <stdlib.h>

int main() {
 
    int a=0, b=0, c=0, maior=0;
    
    scanf("%d %d %d", &a, &b, &c);
    
    maior=(a+b+abs(a-b))/2;
    if(c>maior){
     maior=c;
    }
    
    printf("%d eh o maior\n", maior);
    
    return 0;
}
