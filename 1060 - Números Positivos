#include <stdio.h>
 
int main() {
    double a=0;
    int i=0, count=0;
    for(i=0;i<6;i++){
        if(i<5){
            scanf("%lf\n", &a);
        }
        if(i==5){
            scanf("%lf", &a);
        }
        if(a>0){
            count=count+1;
        }
    }
    printf("%d valores positivos\n", count);
    return 0;
}
