#include <stdio.h>
 
int main() {
 
    int i=0, a=0, count=0;
    for(i=0;i<5;i++){
        scanf("%d", &a);
        if(a%2==0){
            count=count+1;
        }
    }
    printf("%d valores pares\n", count);
    return 0;
}
